#include <bits/stdc++.h>

#define MAXN 100005
#define repn(_,n) for(auto _{0}; _ < n; _++)

using namespace std;

struct Node{
	int clase, subclase, time;

	Node(C, S, T) clase(C), subclase(S), time(T) {}
};

struct Pack{
	int value, time, from, to;

	Pack(N) value(N), from(-1), to(-1) {}
};

vector<Node*> a;

int main(){
	int n, m;
	cin >> n >> m;
	int x, y, aux;
	repn(i,m){
		cin >> x >> y >> aux;

	}
	a.assign(n, nullptr);
	return 0;
}
